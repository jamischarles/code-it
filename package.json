{
  "name": "code-it",
  "version": "1.0.0",
  "description": "Collab code editing",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel public/working.html",
    "deploy": "parcel build public/working.html && env NODE_TLS_REJECT_UNAUTHORIZED=0 firebase deploy"
  },
  "author": "Jamis Charles",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "prismjs": "^1.20.0",
    "render": "^0.1.4"
  }
}
